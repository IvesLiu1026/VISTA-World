# Alpine Villa R3

Session: current user-directed Codex implementation, solo; no delegated writers.
Worktree: `/data/sysx/vista-world/worktrees/vista-alpine-world-r3`.
Branch: `codex/vista-alpine-world-r3`, based on integration `7b4560c19803b3878e10f3014e7fd1d547a87658`.

Owns: R3 scene/asset/animation authoring under `tools/{blender,ue,runtime}/vista_alpine_r3`,
narrow Villa/Embodied plugin changes needed for locomotion, outdoor materials and
validation, focused tests, `docs/design/vista-alpine-r3`, this record and relevant
workflow skill additions, plus the narrow Alpine cache allowlist addition in
`tools/runtime/vista_photoreal_r1/review_session.py` used by this worktree's launcher. Source and binary assets for R1/R2/R5 stay untouched.

Delivery follow-up also owns the corresponding mocked `test_review_session.py`
regression for read-only frozen assets and the separate virtual-texture cache.

Runtime ownership: fresh local private projects and bounded Blender/Unreal jobs in
`/data/sysx/vista-world/runs/vista-villa-r3-20260910a`. Existing session authorization
covers local modeling, GPU proofs, demo isolation, free source assets, commits,
push, review and merge to the integration branch. No paid model/asset calls.

Must not touch: live Home/Villa game and input until explicit revision selection;
older worktrees and frozen projects; other GPU jobs; VISTA ports 8000/8001; main;
datasets, credentials and unrelated user progress. Register R3 separately and only
reload Sunshine while FREE, preserving previous app entries.

Target: a connected, playable Alpine lakeside exterior with natural-looking
interior/exterior light, improved character surfaces and walk/run/jump/land motion.
GTA is the visual-quality reference; acceptance is based on native close/distant
views, measured movement/contact, performance and visible limitations.

Validation: required contract/compiler baseline, relevant focused tests, Editor
and Game builds, private native motion/terrain/doorway/interaction tests and actual
Unreal visual review, frozen/hash-bound delivery and PR to the integration branch.

Implementation source: `95f8a28b`; frozen-launcher cache correction: `901843c7`.
Final native movement, motion and kitchen runs share the five delivery hashes.
Frozen package: `demo-project-a`; profile: `demo-a/sunshine-profile.json`.
Sunshine registration is complete (10 apps, original 9 preserved), unselected;
R1 game/input PIDs remained 3852019 / 3853922 after Sunshine restarted while FREE.
Evidence: `docs/design/vista-alpine-r3/implementation-manifest.json` and the
run-root `sunshine-registration-final.json`. No Moonlight client test was done.
