# Moonlight black-screen recovery

Owner: this user-directed Codex task, solo.
Worktree: `/data/sysx/vista-world/worktrees/vista-moonlight-recovery-r3`.
Branch: `codex/vista-moonlight-recovery-r3`, integration base `c5da9df3`.

Owns: scoped streaming diagnostics/launch fixes and tests under `tools/runtime`,
recovery documentation, and reversible Sunshine application/encoder/display
configuration needed to fix the reported Windows black screen. Evidence belongs
in `/data/sysx/vista-world/runs/vista-moonlight-recovery-20260910a`.

Latest user request authorizes repair of the broken streaming session. Preserve
all frozen demo assets and archive previous launch entries before simplifying
the menu. Keep auth, other desktop services, research/training GPU processes,
production ports, main, and unrelated worktrees untouched. A Sunshine restart
may briefly interrupt this broken session; do not restart any other service.

Validate the actual captured desktop, encoder path, a client retry, current
project identity and input service; do not infer Windows success from a PNG.

Recovery accepted from the user's Windows performance overlay on 2026-09-10:
1920x1080 H.264, 59.38 FPS received/decoded/rendered, 0% network frame loss,
1 ms network latency after a 4000 Kbps encoding cap. Preserve this live session.
The prior visible session received 5.59 FPS with 92.03% network frame loss.
The exact congested hop and the initial transition out of black remain unknown.

Final configuration remains Sunshine 2026.516.143833, X11 :119, software
libx264/ultrafast/zerolatency, H.264, max_bitrate=4000. A newer official AppImage
was downloaded and checksum-verified as preparation, but was not activated.
Captured video IP packets were 1068 bytes without fragmentation; no packet-size
override, MTU, firewall, authentication or other desktop-service changes applied.
Temporary packet capture is stopped. The R3 game/input PIDs survived the bitrate
restart. Applications were reduced from ten to three with exact backups and
unchanged retained entries. See docs/runbooks/vista-moonlight-recovery-r3.md.
